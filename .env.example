# Claude Control Panel Configuration

# Server
HOST=127.0.0.1
PORT=3847

# Claude Workspace root (per-agent directories live here)
CLAUDE_WORKSPACE=~/claude-workspace

# Primary agent id + display name (defaults to overseer/Overseer in code)
PRIMARY_AGENT_ID=ghost
ASSISTANT_NAME=Ghost

# Claude model
MODEL=claude-opus-4-5-20251101

# Auth token (required)
CCP_AUTH_TOKEN=change-me

# Optional: restrict browser origins (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3847,http://127.0.0.1:3847

# Limits
MAX_WS_PAYLOAD_MB=10
UPLOAD_MAX_MB=10
HISTORY_BLOCK_LIMIT=300

# Logging
INFO_LEVEL=info

# Web tools (disable by default for safety)
WEB_TOOLS_ENABLED=false
